# Always use the python docs to find all the info!!

list1 = [1, 5, 3, 7, 86, 420, 69]
print(list1)
# list1.sort()  # sorts the list
# list1.reverse()  # reverses the list
# list1.append(669)  # adds the elements at the end of the list.
# list1.insert(3, 5)    # Inserts the element at the specific position here, first value is the index where we will be adding the value and second is the value itself.
# list1.pop(3)    # it will delete the element at the 3rd index
# list1.remove(86)  # it will remove the specified value and no need to specify the index
print(list1)
