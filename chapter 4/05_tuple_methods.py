# Creating a tuple using ()
t1 = (1, 2, 3, 4, 4, 4, 5, 5)

print(t1.count(4))  # Gives the count of the element specified.
print(t1.index(5))  # Gives the index of the first occuring element specified.
