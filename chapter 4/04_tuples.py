# Creating the tuples using ()
t1 = (1, 2, 3, 4, 5)
# t1 = ()   # Empty tuple
# t1 = (1,) # Tuple with an single element always give comma after the element, but 'only' in case of the 'tuple with single element'.

# Prints the tuples
print(t1)
print(t1[2])

# We cannot change the values of tuples basically they are immutable unlike lists
# t1[4] = 15    # It will throw an error
print(t1)
